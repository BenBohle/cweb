#!/usr/bin/make -f

%:
	dh $@ --buildsystem=cmake --builddirectory=build

override_dh_auto_configure:
	dh_auto_configure --builddirectory=build -- \
		-DCMAKE_BUILD_TYPE=Release \
		-DCWEB_CLI_USE_LOCAL_FRAMEWORK=OFF
